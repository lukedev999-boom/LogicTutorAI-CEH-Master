# 題目圖片存放處
#
# 依專案設定，圖片不進版控，部署時再另外放置（此目錄結構本身保留）。
#
# 題庫 md 中的圖片路徑寫法不拘 —— ../images/q1.png、images/q1.png、
# 甚至只寫 q1.png，程式都會正規化為指向本目錄，把檔案平放進來即可。
# 若要分子目錄，寫成 ../images/2024/q1.png，images/ 之後的結構會被保留。
*
!.gitignore
